#include <vector>
#include <stdio.h>

using namespace std;

int main() {
  // vector<int> a;
  // a.reserve(16);
  // int e = 1;
  // a.push_back(e);
  // for (int i = 0; i < a.size(); ++ i) {
  //   printf("%d\n", a[i]);
  // }
  nullptr_t a = nullptr;

  return 0;
}